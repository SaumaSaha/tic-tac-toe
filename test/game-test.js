const {describe, it} = require("node:test");
const {deepStrictEqual} = require("assert");

describe("Game", () => {
  describe("start", () => {
    it("should start and ask for player names", () => {
      const game = new Game()
    });
  });
});